{
  "name": "shoot-scheduling-api",
  "version": "0.4.2",
  "description": "Legacy scheduling service for booking soundstages for shoots. Brownfield demo for the coding-agent workshop.",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "echo \"No tests yet. This is part of the point.\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
